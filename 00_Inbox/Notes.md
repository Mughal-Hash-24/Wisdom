# Artificial Intelligence
## State Space Graphs
{Explain in detail what exactly is a space state graph: A mathematical representation of a search graph where nodes are search configs arcs are successors and goal nodes. Make sure you use real world examples and mermaid diagrams for explanations as well}
## Search Tree
{What exactly is a search tree and why do we convert a graph to a search tree. What benefits does it serve. Explain using each arch as a "What if". Start node is the root. Why can't we make a complete tree for most problems}
{Use side by side difference tables to differentiate between A search graph and search tree}
{Make to mermaid diagrams for one search problem. One represents its search graph and one represents a search tree}
{Differentiate btw the graph and trees' time and space complexities in the scope of searching problems}
## DFS
{Explain DFS in the scope of a search problem and then perform an example run on a search graph and tree using DFS}
{Explain to me each and every property of a search algorithm in detail}
